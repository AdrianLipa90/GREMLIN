# GREMLIN / BELZEBUB Complex-Overlap Frame Bridge v2.3

Status: `CANDIDATE / CONDITIONAL BRIDGE CONSTRUCTION`

## Source structure

The July symbolic formalism supplies a complex Hilbert space with orthonormal symbolic basis states and defines resonance by

\[
R(S,I)=|\langle S|I\rangle|^2.
\]

A later geometric source separately identifies neutrino species among the fixed-point structure and treats PMNS mixing as a residual flag-phase prediction.

The v2.3 question is whether the missing bridge must remain an arbitrary unitary matrix, or whether it can be constructed from the already available complex overlap amplitudes once the neutrino triple is source-bound.

## Conditional construction

Assume that the physical embedding supplies a labelled orthonormal triple

\[
\{|S_{\nu_e}\rangle,|S_{\nu_\mu}\rangle,|S_{\nu_\tau}\rangle\}\subset H_I.
\]

Define the analysis operator

\[
C_\nu:H_I\rightarrow\mathbb C^3,
\qquad
(C_\nu I)_\alpha=\langle S_{\nu_\alpha}|I\rangle.
\]

On the span of this orthonormal triple, `C_nu` preserves norm. If the flavour and mass bases are related by the unitary PMNS matrix, the mass-amplitude bridge is then

\[
\boxed{J_m=U_{\rm PMNS}^\dagger C_\nu.}
\]

No additional free `U(3)` matrix is inserted. Once the labelled frame and PMNS orientation are fixed, the map is determined up to ordinary basis-phase conventions.

## Why scalar resonance was insufficient

The familiar resonance values are only

\[
R_\alpha=|\langle S_{\nu_\alpha}|I\rangle|^2.
\]

They discard the relative complex phases between overlap amplitudes. v2.3 includes a negative control with two states that have exactly the same component probabilities but a nonzero relative phase. Their `R_alpha` vectors are identical, yet after the same `U_PMNS^dagger` transformation they produce different mass amplitudes.

Therefore

\[
\boxed{R\text{ is the probability shadow of the bridge, not the bridge itself}.}
\]

The complex overlaps must be retained.

## Embedding firewall

The current source set does not yet explicitly prove that the geometric neutrino fixed-point triple is the same labelled orthonormal triple inside the symbolic/intention Hilbert space used by `R(S,I)`.

Accordingly v2.3 distinguishes:

- the mathematical construction, which is closed once such a frame is supplied;
- the physical embedding/provenance step, which remains open.

The module also keeps the PMNS orientation source explicit rather than silently identifying any generic three-component basis with the flavour basis.

## BELZEBUB verdict

Conditionally,

\[
\boxed{
H_I\xrightarrow{C_\nu}H_F\xrightarrow{U_{\rm PMNS}^\dagger}H_m
}
\]

replaces the arbitrary `J in U(3)` debt by a narrower source-binding problem.

Current status:

\[
\boxed{\text{arbitrary matrix freedom structurally removed; physical cross-Hilbert embedding still open}.}
\]

The next evidential target is not another free matrix parameter. It is an explicit source-grounded identification of the three neutrino geometric modes with the three orthonormal overlap modes in the same intention/symbolic Hilbert space, together with declared PMNS basis orientation.
