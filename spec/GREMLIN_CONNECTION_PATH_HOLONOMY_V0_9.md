# GREMLIN Connection-Path Holonomy Closure v0.9

Status: **IMPLEMENTATION CANDIDATE** pending CI.

## Dependency chain

`Lambda_R -> effective source energy -> geometry adapter -> projected connection on closed loop -> path integral -> wrapped holonomy phase -> QHTRI tau_ij -> epsilon_ij`.

## Connection path integral

For a closed loop `gamma` with a declared U(1) phase projection of the bound connection, v0.9 evaluates the discrete line integral

`Phi_hol = sum_k omega_parallel,k * ds_k`

and then

`tau_holonomy = wrap_pi(Phi_hol)`.

The path receipt binds the Lambda-energy lineage, metric commitment, connection commitment, geometry-adapter identity, loop identity, projected connection samples, segment lengths, evidence source and epistemic status.

`manual_tau_present=false` is part of the sealed receipt. The QHTRI lag is therefore inherited from the connection-path integral.

## QHTRI bridge

v0.9 reuses the validated v0.8 QHTRI torsion law

`epsilon = wrap_pi(n*theta_i - m*theta_j - tau_holonomy)`

and preserves the exact integer pair `(n,m)` as operator identity.

## Geometry lineage

The path-integral receipt is converted into the v0.8 relational-geometry holonomy receipt by passing the derived raw line integral as the holonomy input. The resulting v0.9 wrapper binds both commitments, preserving the chain from Lambda-energy source to connection transport and QHTRI phase torsion.

## Entanglement validation frontier

The connection-derived phase supplies a physical candidate for correlated phase transport. Quantum entanglement promotion requires a joint-state construction and a non-separability witness. The v0.9 receipts therefore retain `OPEN_REQUIRES_QUANTUM_WITNESS` until that validation lane exists.

## Falsification tests

The v0.9 test lane checks:

- deterministic path integration;
- principal-phase wrapping;
- path subdivision invariance for constant projected connection;
- rejection of open loops;
- rejection of negative segment lengths and mismatched arrays;
- commitment/tamper detection;
- exact propagation of the derived holonomy phase into QHTRI `tau` and `epsilon`;
- preservation of exact integer winding identity without gcd normalization.

## Next closure

The next mathematical dependency is the explicit geometry adapter that derives the projected spin/phase connection from a metric/tetrad generated by the relational source field. That step will replace the current upstream connection witness with a reproducible metric -> tetrad -> spin connection -> path projection pipeline.
